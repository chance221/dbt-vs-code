sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        config:
          loaded_at_field: _batched_at
          freshness:
            warn_after: 
              count: 24 
              period: hour
            error_after: 
              count: 48 
              period: hour
          #-- IGNORE --
          # You can utilize codegen at hub.getdbt.com/dbt-labs/codegen/latest to generate this source config for you.
          #It can generate base models and test as well. 
